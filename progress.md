# 작업 진행 기록

## 전체 요약

**완료된 것 (총 6개 작업, 커밋 7개)**
1. **아카이브(퀘스트 로그)** — `#/archive`, `#/archive/{slug}` 라우팅,
   카테고리 그룹 + 검색 + 키보드 내비게이션 2단 패널. 데스크톱/태블릿/모바일,
   키보드 전용, 직접 URL/새로고침/뒤로가기까지 전부 실제로 브라우저에서
   테스트 완료. 구현 중 모바일 "뒤로가기" 버튼이 안 보이는 버그를 발견해
   수정함 (`data-mobile-view` 속성 위치 문제).
2. **카테고리 자동 정리** — 34개 포스트 중 29개에 내용 기반으로 카테고리
   부여 (HTB 20 / 웹 해킹 3 / 네트워크 2 / 암호학 1 / 신규 "시스템 해킹" 3).
   애매한 5개(`db`, `tools`, 개인 메모성 글 3개)는 미분류로 유지.
3. **위키링크(`[[...]]`, `![[...]]`) 처리** — 대괄호를 벗겨 일반 텍스트로
   표시. 코드 블록 안의 문법 예시는 보존. 카드 excerpt에 섞여 있던 것도
   함께 정리.
4. **Hero 이미지 점검** — 알파 채널 전수 분석 + 육안 확대로 검/캐릭터/용
   몸통에 구멍이 없음을 확인. **재생성 불필요.**
5. **성능/접근성 점검 (Lighthouse 실측)** — Performance 74 / Accessibility
   95 / Best Practices 96→**100**(favicon 404 수정) / SEO 100. 성능 저하의
   주원인(비압축 PNG, ~3MB)과 카드 텍스트 4곳의 WCAG AA 대비 미달을 정확한
   수치로 특정했으나, 색상 토큰 변경이나 이미지 파이프라인 신설처럼 이번
   범위를 벗어나는 디자인 결정이 필요해 보류 (아래 "확인 필요" 참고).
6. **`.gitignore` 생성** — OS/에디터/임시파일/로그 표준 패턴. 점검 중
   발견한 무관 파일(`scroll-card.png_files/`, 브라우저 저장 부산물
   9.4MB)은 사용자 확인 후 삭제 완료.

**확인/판단이 필요한 것 (사용자 결정 대기)**
- 카테고리 미분류로 남은 5개 글(`db`, `tools`,
  `jigjeob-chigi-gwichanheun-myeongryeongeo`, `obsidieon-jeongribeob`,
  `oscp-junbi-peulraen`) — 새 카테고리를 만들지, 미분류로 둘지
- 위키링크 변환 규칙(파이프 뒤 별칭 우선 등)이 현재 34개 글의 실제
  패턴만 보고 정한 것이라, 새로운 형태가 나오면 재검토 필요
- 포스트 카드 4개 요소의 색상 대비 WCAG AA 미달 (`--accent-violet`,
  `--accent-gold`, `--text-muted`를 밝은 배경 위 작은 텍스트에 직접
  사용) — 토큰 값을 건드릴지, 해당 요소만 예외 처리할지
- hero 이미지 3장 + 테두리 이미지 2장, 총 ~6.2MB의 비압축 PNG를 WebP로
  전환할지 (CLAUDE.md 목표치인 hero 300–500KB와 현재 격차가 큼)
- `prefers-reduced-motion` 실제 OS 토글 상태에서의 수동 확인은 못함
  (코드 리뷰로는 모달과 동일 패턴이라 정상 작동 예상)

**다음에 하면 좋을 것 (제안)**
1. 이미지 최적화(WebP 변환 + 압축)부터 처리하면 Performance 점수가
   가장 크게 개선될 것으로 보임 (현재 병목의 거의 전부가 이미지 배송)
2. 카드 텍스트 대비 문제는 작은 시각적 변경이라 다음 세션에서 바로
   결정·적용 가능
3. "카테고리" / "소개" / "검색" 네비게이션 항목은 아직 `href="#"` 상태로
   미구현 — 아카이브 다음으로 우선순위가 높아 보임

---

## 작업 로그

### 완료: 1. 아카이브(퀘스트 로그) 구현

**변경 파일**
- `index.html`: nav의 "아카이브" 링크를 `#/archive`로 연결(`id="nav-archive-link"`),
  아카이브 backdrop/dialog 마크업 추가, `archive.js` 스크립트 태그 추가
- `assets/js/archive.js` (신규): 아카이브 전체 로직
- `assets/css/style.css`: `.archive*` 스타일 섹션 추가 (기존 규칙은 수정하지 않고 append)

**판단 근거**
- 기존 포스트 모달(`assets/js/modal.js`, `posts.js`)은 전혀 수정하지 않았습니다.
  "재사용"은 동일한 상호작용 패턴(포커스 트랩 / ESC·backdrop 닫기 / 스크롤 락)을
  의미한다고 해석해, `archive.js`에 동일 패턴을 독립적으로 재구현했습니다. 두 시스템이
  같은 전역 클래스(`no-scroll`)를 쓰지만 동시에 열리지 않도록 `openArchive()`에서
  `PostModal.isOpen()`이면 먼저 닫도록 처리했습니다. 이렇게 하면 `posts.js`의 기존
  해시 라우팅(`#slug`)과 아카이브의 해시 라우팅(`#/archive`, `#/archive/slug`)이
  서로 다른 패턴이라 충돌하지 않습니다.
- 시각적으로는 "두루마리 스타일"과의 일관성을 위해 모달과 동일한
  `scroll-modal.png` 테두리 이미지를 재사용했습니다. 마크다운 렌더링 스타일
  (`modal__body ...`)은 기존 규칙을 건드리지 않기 위해 `archive__detail-body`에
  별도로 복제했습니다 (약간의 CSS 중복은 있지만 기존 스타일 손상 위험 회피).
- 카테고리 그룹은 네이티브 `<details>/<summary>`로 구현 — 별도 JS 없이 키보드/접근성
  기본 동작(Enter/Space 토글) 확보.
- 목록 항목은 `<a href="#/archive/slug">`로 구현해 실제 permalink 역할도 겸함.
- 모바일 1단 패널 전환은 `#archive-dialog`(둘러싸는 dialog 엘리먼트)에
  `data-mobile-view="list|detail"` 속성을 두고 CSS로 제어.

**버그 수정 (테스트 중 발견)**
- 최초 구현에서 모바일 "뒤로가기" 버튼이 계속 안 보이는 문제 발견 → 원인은
  `data-mobile-view`를 `#archive-body`(리스트/디테일 패널의 부모)에 설정했는데,
  `.archive__back` 버튼은 `archive__titlebar`의 자식이라 `archive__body`의
  하위 요소가 아니었음 (형제 관계). CSS 하위 선택자가 매칭되지 않아 버튼이 항상
  `hidden` 상태로 남아 있었음. `data-mobile-view` 속성을 상위 `#archive-dialog`로
  옮기고 CSS 선택자를 `.archive[data-mobile-view=...]`로 수정해 해결.

**테스트 결과 (로컬 서버 + 브라우저 자동화)**
- 데스크톱(1400px+): 2단 패널 정상, 검색 필터링, 카테고리 그룹 접기/펼치기 확인
- 태블릿(768px, iframe으로 뷰포트 강제): 2단 패널 유지 확인
- 모바일(390px, iframe으로 뷰포트 강제): 1단 패널(목록→상세 전환), 뒤로가기
  버튼 노출/동작 확인
- 키보드 전용: 카드 focus → ArrowUp/ArrowDown으로 항목 간 이동, Enter로 선택,
  Escape로 닫힘 + 포커스가 트리거 요소(아카이브 nav 링크)로 정확히 복귀 확인
- 직접 URL 진입(`#/archive`, `#/archive/{slug}`) 및 새로고침 정상 동작 확인
- 브라우저 뒤로/앞으로가기: 아카이브 닫힘 ↔ 열림 ↔ 선택된 글 전환까지 정상 확인
- 콘솔 에러 없음
- `prefers-reduced-motion`: 코드 레벨에서 모달과 동일하게 `matchMedia` 체크로
  트랜지션 duration을 0으로 처리 (실제 OS 설정 토글 테스트는 진행하지 않음 —
  아래 "확인 필요" 참고)

**확인 필요**
- `prefers-reduced-motion: reduce`를 실제로 활성화한 상태에서의 수동 확인은
  이번 세션에서 하지 못했습니다 (코드는 모달과 동일한 패턴이라 정상 동작할
  것으로 예상하지만 실제 OS 설정 토글 테스트는 아님).
- 카테고리 그룹 "미분류(34)"처럼 그룹이 큰 경우 접기 UX는 확인했지만, 그룹이
  아주 많아졌을 때(카테고리 10개 이상) 좌측 리스트 스크롤 UX는 추가 검증 없음.

---

### 완료: 2. 카테고리 자동 정리

**방법**: 34개 포스트 전부의 frontmatter(제목, `Box:` 헤더 유무)와 본문 앞부분을
직접 읽고 분류한 뒤, `posts/*.md`의 `category:` 필드와 `posts/index.json`의
`category` 필드를 스크립트로 일괄 반영했습니다 (수작업 오타 방지를 위해
매핑 테이블 기반 스크립트 사용, 매핑 자체는 사람이 내용을 읽고 판단).

**분류 근거**
- **HTB (20개)**: `access, boardlight, broker, builder, busqueda, cozyhosting,
  dog, editor, help, jeeves, keeper, magic, markup, networked, pandora, sau,
  servmon, soccer, updown, usage` — 전부 본문에 `#### Box: 이름 | OS | 난이도`
  형식의 HTB 머신 풀이 헤더가 있어 명확하게 판별.
- **웹 해킹 (3개)**: `blind-sqli-script`(Blind SQLi 자동화), `file-transfer`
  (웹쉘 업로드용 매직바이트/특수문자 우회), `web-checklist`(웹 정찰 체크리스트).
- **암호학 (1개)**: `hash` — 해시 식별 기준 + hashcat 사용법.
- **네트워크 (2개)**: `enumeration-checklist`(nmap/포트/SNMP 위주 정찰),
  `smb-checklist`(SMB 프로토콜 열거).
- **시스템 해킹 (신규 카테고리, 3개)**: `linux-privesc`, `windows-privesc`,
  `reverse-shell` — 기존 4개 카테고리(웹 해킹/HTB/암호학/네트워크) 중 어디에도
  깔끔히 들어가지 않는 OS 레벨 권한상승·쉘 획득 기법 모음이라 최소한으로 새
  카테고리를 추가했습니다. 세 파일 모두 "박스 하나에 종속되지 않는 범용
  post-exploitation 기법"이라는 공통점이 있어 하나의 카테고리로 묶는 것이
  기존 톤(주제별 체크리스트)과 가장 일관된다고 판단했습니다.

**미분류로 남긴 5개 (애매함)**
- `db`: PostgreSQL 클라이언트 명령어 모음 — 웹/네트워크/암호학/시스템 중
  하나로 단정하기 애매함 (DB 피벗팅은 여러 카테고리에 걸침).
- `tools`: searchsploit, hydra, ssh 터널링, mdbtools/readpst, KeePass 크랙,
  puttygen 등 성격이 다른 도구 모음이라 단일 카테고리로 묶기 부적절.
- `jigjeob-chigi-gwichanheun-myeongryeongeo`: HTB VPN 접속 명령어 한 줄짜리
  메모 — 보안 기술 콘텐츠라기보다 개인 유틸리티 메모.
- `obsidieon-jeongribeob`: Obsidian 폴더 구조 정리법 — 보안 콘텐츠가 아닌
  메타 노트.
- `oscp-junbi-peulraen`: OSCP 학습 일정표 — 기술 콘텐츠가 아닌 개인 계획 노트.

이 5개는 사용자가 직접 판단해서 분류하거나 별도 카테고리(예: "메모"/"스터디
플랜")를 만들지 결정하는 것이 나을 것 같아 임의로 카테고리를 부여하지
않았습니다.

---

### 완료: 3. 위키링크([[...]], ![[...]]) 처리

**문제**: `posts/*.md` 34개 중 다수가 Obsidian 위키링크 문법(`[[페이지명]]`,
`![[이미지|크기]]`)을 그대로 쓰고 있는데, `posts/` 안에는 링크 대상 `.md`나
이미지 파일이 실제로 하나도 없어서 (이미지들은 `assets/images/`에만 있음)
marked.js가 이를 인식하지 못하고 브래킷이 그대로 텍스트로 노출되고 있었음.

**변경 파일**
- `assets/js/posts.js`, `assets/js/archive.js`: 각각 raw 마크다운을
  `marked.parse()`에 넘기기 전에 `cleanWikilinks()`를 거치도록 추가
  (`stripFrontmatter()` 다음 단계로 자연스럽게 이어붙임 — 기존 전처리
  파이프라인 패턴을 그대로 따름)
- `posts/index.json`: 카드에 노출되는 `excerpt` 필드 4곳에 섞여 있던
  위키링크 브래킷도 동일 규칙으로 정리

**구현 방식과 판단 근거**
- `modal.js`는 건드리지 않았습니다. 위키링크 정리는 마크다운 원문 단계에서
  처리하는 편이 안전해서, 이미 `stripFrontmatter()`로 원문을 전처리하고 있는
  `posts.js`/`archive.js`에 같은 방식으로 추가했습니다.
- **코드 블록 보존이 핵심 이슈**였습니다: `obsidieon-jeongribeob.md`에는
  Obsidian 링크 문법 자체를 설명하는 예시(````[[OSCP/cheatsheets/linux_privesc.md]]````)가
  펜스 코드 블록(`~~~`) 안에 실제로 들어 있어서, 이 텍스트까지 건드리면 안 됩니다.
  그래서 브래킷 제거를 라인 단위로만 적용하고, ` ``` `/`~~~` 펜스 시작~종료
  구간 안에서는 원문을 그대로 보존하도록 `cleanWikilinks()`를 구현했습니다.
  (인라인 코드 스팬 안에는 실제 위키링크 사례가 없어 별도 처리하지 않았습니다 —
  전체 posts 검색으로 확인함)
- **내부 링크 기능은 만들지 않았습니다** (요청대로). `posts/` 안에 실제
  대상 파일/이미지가 하나도 없다는 걸 먼저 확인했고, 34개 파일의 모든
  `[[...]]`/`![[...]]` 사용례를 grep으로 전수 조사한 결과 예외 없이 전부
  "대상 없음" 케이스였습니다. 따라서 "대상이 있으면 링크, 없으면 텍스트"라는
  조건부 로직 대신 — 어차피 항상 텍스트 경로로 귀결되므로 — 항상 브래킷만
  벗겨내는 단순한 규칙으로 구현했습니다. 실제로 `posts/`에 매치되는 콘텐츠가
  생기면 이 부분은 재검토가 필요합니다.
- 변환 규칙: `![[파일명|크기]]` → `파일명` (이미지 크기 힌트는 텍스트로서
  의미가 없어 제거), `[[페이지|별칭]]` → `별칭` (Obsidian 관례상 파이프 뒤가
  표시용 별칭), `[[페이지#섹션]]` → `페이지#섹션` 그대로 (별도 예시가 없어
  가장 보수적으로 원문 유지).

**테스트 결과**
- `hash.md` 본문의 `[[keeper]]` → "keeper"로 정상 렌더링 확인 (아카이브
  상세 패널)
- `broker.md`의 `![[Pasted image ...|472]]` → "Pasted image
  20260717145514.png"로 정상 렌더링 확인 (브래킷/느낌표/크기 힌트 모두 제거)
- `obsidieon-jeongribeob.md`의 코드 블록 안 예시
  `[[OSCP/cheatsheets/linux_privesc.md]]`가 브래킷 그대로 보존되는 것을
  확인 (의도적 예시 텍스트 보호)
- `posts/index.json`에 더 이상 `[[` 패턴이 없음을 grep으로 확인 (카드
  excerpt 정리 완료)

**확인 필요**
- 위 변환 규칙은 현재 데이터셋(34개 포스트)의 실제 사용 패턴을 전수 조사해서
  만든 규칙이라, 새 글에서 지금까지 없던 위키링크 형태(예: 파이프가 있는
  일반 링크, 인라인 코드 스팬 안의 위키링크)가 나오면 예상과 다르게 보일 수
  있습니다.

---

### 완료: 4. Hero 이미지 최종 점검 (`traveler-cliff.png`, `palace-dragon.png`)

**검사 방법** (수정은 하지 않고 점검만 진행)
1. 알파 채널 전수 분석: `scipy.ndimage.label`로 각 이미지의 투명 영역을
   연결성분 분석 → 바깥 배경과 연결되지 않은("갇힌") 투명 영역이 있으면
   그게 곧 "캐릭터 안에 뚫린 구멍"이라 판단할 수 있음.
   → **`traveler-cliff.png`: 갇힌 투명 영역 0개, `palace-dragon.png`: 갇힌
   투명 영역 0개.** 배경 제거 과정에서 검/캐릭터/용 몸통을 관통해 구멍이
   뚫린 흔적은 없음.
2. 검 부분 3배 확대 크롭으로 육안 확인 → 칼날, 손잡이, 등에 멘 보조 무기
   (지팡이로 추정)까지 픽셀 끊김이나 투명 구멍 없이 온전함.
3. 용 몸통(S자 곡선) 확대 확인 → 목~꼬리까지 연속적으로 이어져 있고 구멍 없음.
4. (부가 확인, 오탐 기록) `palace-dragon.png`의 산맥/구름 영역 일부에서
   체커보드 형태의 패턴을 발견해 처음엔 "배경 제거 아티팩트"로 의심했으나,
   알파값이 전부 255(완전 불투명)이고 두 색상이 규칙적으로 번갈아 나오는
   형태라 픽셀아트에서 흔히 쓰는 디더링(하프토닝) 음영 기법으로 판단됨 —
   실제 문제 아님.

**결론: 재생성 필요 없음 (재생성 불필요)**
- 사용자가 우려한 "배경 제거 중 검 부분이 함께 지워지는 문제"는 현재
  커밋된 파일에서는 재현되지 않습니다. git log상 두 파일 모두 최초 커밋
  이후 수정 이력이 없어(`35964c7` 한 번뿐), 문제가 있었다면 이미 그 시점
  이전에 (커밋 전 로컬 편집 단계에서) 해결되었거나, 원래 우려와 달리
  발생하지 않았던 것으로 보입니다.

**확인 필요**
- 이번 점검은 정적 파일 분석(알파 채널 홀 탐지 + 육안 확대)만 진행했고,
  실제 브라우저 hero 렌더링에서 패럴럭스 스케일/변형 시 확대·축소로 인해
  구멍이 더 도드라지는지는 별도로 보지 않았습니다 (다만 CSS
  `image-rendering: pixelated`라 스케일 아티팩트 위험은 낮다고 판단됩니다).

---

### 완료: 5. 성능/접근성 최종 점검 (Lighthouse)

**방법**: `npx lighthouse`로 로컬 서버(`http://localhost:8811`)를 headless
Chrome으로 감사. 실제 CLI 실행 결과이며 추정치가 아닙니다.

**최초 점수**
| 항목 | 점수 |
|---|---|
| Performance | 74 |
| Accessibility | 95 |
| Best Practices | 96 |
| SEO | 100 |

**콘솔 에러 → 수정 완료**
- `errors-in-console` 감사에서 유일하게 잡힌 항목: `favicon.ico` 요청이
  404 (파일 없음). `<head>`에 인라인 SVG data URI favicon을 추가해 해결
  (별도 이미지 파일/네트워크 요청 없이 즉시 해결, 사이트의 골드/네이비
  팔레트로 작은 룬 모양 아이콘 구성 — CLAUDE.md의 "미묘한 빛나는 룬" 모티프와
  일관되게 판단).
- 수정 후 재감사: **Best Practices 96 → 100**, 콘솔 에러 0건 확인.

**성능 (Performance 74) — 원인 진단, 수정은 하지 않음**
- LCP 20.1s / TTI 20.4s로 매우 느림. `image-delivery-insight` 감사가
  "이미지 배송 개선 시 약 3,494 KiB 절감 가능"을 지목 — 원인은
  `sky.png`/`palace-dragon.png`/`traveler-cliff.png`(각 ~1MB, 총 hero
  이미지만 ~3MB)와 `scroll-card.png`(2.1MB)/`scroll-modal.png`(1.1MB) 같은
  비압축 PNG들입니다.
- 이는 CLAUDE.md가 이미 명시한 요구사항("Prefer compressed WebP images",
  "초기 hero 이미지 300–500KB 목표")과 정확히 어긋나는 지점입니다. 다만
  이번 태스크 5는 "점검"까지만 요청받았고, 실제 WebP 변환/압축은 이미지
  자산 파이프라인을 새로 만드는 별도 작업 범위라고 판단해 **직접 변환하지
  않았습니다** — 아래 "확인 필요"에 남깁니다.
- 그 외 마이너: 미압축 CSS/JS(합쳐서 ~7KB 절감), 사용 안 하는 CSS/JS
  (~41KB), render-blocking 리소스 최적화(~1s 절감 여지) — 전부 지금
  규모에서는 우선순위 낮음.

**접근성 (Accessibility 95) — 색상 대비 문제 발견, 미수정**
- 유일한 실패 항목: `color-contrast`. 포스트 카드의 4개 텍스트 요소가
  전부 `--panel-bg: #f4f1e7`(밝은 배경) 위에서 WCAG AA 기준(4.5:1)을
  충족하지 못함:
  - `.post-card__category` (`--accent-violet #8c7cf0`): 대비 2.97
  - `.post-card__date` (`--text-muted #687087`): 대비 4.36 (기준 4.5에
    근소하게 미달)
  - `.post-card__excerpt` (`--text-muted #687087`): 대비 4.36 (동일)
  - `.post-card__reading-time` (`--accent-gold #f1c75b`): 대비 1.42
    (심각하게 미달)
- **왜 직접 고치지 않았는가**: 원인이 CLAUDE.md에 하드코딩된 색상 토큰
  자체(`--accent-violet`, `--accent-gold`, `--text-muted`의 실제 hex 값)를
  작은 텍스트에 그대로 쓰기 때문입니다. "색상 토큰... 절대 벗어나지 마"라는
  명시적 규칙이 있어서, 토큰 값 자체를 임의로 바꾸는 건 이번 작업 원칙과
  충돌한다고 판단했습니다. 토큰 값을 바꾸지 않고 고치려면 (a) 이 네 요소에만
  더 어두운/진한 별도 색을 적용하는 예외를 만들거나, (b) 폰트 크기를
  키워 "large text" 기준(3:1)을 적용받게 하는 등 브랜드 톤에 영향을 주는
  판단이 필요해서, 제가 임의로 결정하지 않고 아래 "확인 필요"에 남겼습니다.

**이미지 lazy-loading 적용 여부**
- 현재 사이트에는 `<img>` 태그가 실제로 렌더링되는 곳이 없습니다: hero는
  CSS 배경 레이어(3장, 스크롤 위 최우선 콘텐츠라 오히려 즉시 로드가 맞음),
  카드/모달 테두리는 `border-image`이고, 포스트 본문은 표준 마크다운
  이미지 문법(`![alt](url)`)을 쓰는 글이 하나도 없어(전부 Obsidian
  위키링크였고 3번 작업에서 텍스트로 정리됨) `<img>`가 생성되지 않습니다.
  → **지금은 지연 로딩을 적용할 대상 자체가 없어 위반 사항 없음.** 다만
  앞으로 글에 실제 이미지가 들어가면(`![alt](image.png)` 형태) marked.js가
  생성하는 `<img>`에 `loading="lazy"`가 자동으로 붙지 않으므로, 그 시점에는
  렌더러 커스터마이징이 필요합니다 (아래 "확인 필요").

**확인 필요**
1. **이미지 자산 최적화**: hero 3장 + 카드/모달 테두리 2장, 총 ~6.2MB의
   비압축 PNG를 WebP로 변환하고 CLAUDE.md 목표치(hero 300–500KB)에 맞게
   압축할지 결정 필요. 이번 세션에서는 변환 도구/품질 기준을 사용자와
   맞추지 않고 임의로 손대는 게 위험하다고 판단해 보류했습니다.
2. **카드 텍스트 색상 대비**: 위 4개 요소의 WCAG AA 미달을 어떻게
   해결할지 (토큰 자체를 조정 / 해당 요소만 예외 색상 적용 / 폰트
   크기·굵기 조정) 디자인 판단이 필요합니다.
3. **향후 이미지 지연 로딩**: 글 본문에 실제 이미지가 추가될 경우
   marked.js의 이미지 렌더러를 커스터마이징해 `loading="lazy"`를 자동으로
   붙이는 작업이 필요합니다 (지금은 대상이 없어 미구현).
4. ~~**저장소 용량 이슈 (부가 발견)**: `assets/images/scroll-card.png_files/`
   디렉터리(41개 파일, 9.4MB)가 git에 커밋되어 있는데...~~ →
   사용자 확인 후 삭제 완료 (아래 6번 항목 참고).

---

### 완료: 6. `.gitignore` 생성 + 무관 파일 정리

**변경 파일**
- `.gitignore` (신규): OS(`.DS_Store` 등) / 에디터(`.vscode`, `.idea`,
  swap 파일) / Node 툴링(`node_modules` 등 — 지금은 빌드 스텝이 없지만
  나중에 추가될 경우를 대비) / 임시 파일 / 로그 표준 패턴
- `assets/images/scroll-card.png_files/` (41개 파일, 9.4MB) 삭제

**판단 근거**
- 5번 작업(성능 점검) 중 이 디렉터리를 발견했습니다. 파일명 패턴
  (`ADAVisualizationComponent-*.css`, `AnimatedMascot-*.css`, `sdk.js`,
  UUID 이미지들, `frame.html`)이 정확히 Chrome "웹페이지, 전체 저장"
  기능이 만드는 부산물 구조와 일치하고, 사이트 코드 어디서도 참조하지
  않으며(`scroll-card.png` 파일 자체만 CSS `border-image`로 쓰임), git
  최초 커밋 때부터 들어있던 걸 확인했습니다.
- 처음에는 "관련 없는 파일은 삭제하지 말라"는 원칙과 이번 6개 작업
  범위를 벗어난다는 이유로 삭제하지 않고 progress.md에만 기록해뒀는데,
  사용자가 직접 삭제를 지시해 `git rm -r --cached`로 정리했습니다.
- `package-lock.json`은 gitignore에 넣지 않았습니다 (지금은 npm을 안
  쓰지만, 나중에 쓰게 되면 재현성을 위해 커밋하는 게 일반적이라 미리
  막아두는 게 오히려 표준 관행에 어긋난다고 판단).

**확인 필요**
- 없음.
